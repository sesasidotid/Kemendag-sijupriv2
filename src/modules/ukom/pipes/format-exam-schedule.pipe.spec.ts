import { FormatExamSchedulePipe } from './format-exam-schedule.pipe';

describe('FormatExamSchedulePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatExamSchedulePipe();
    expect(pipe).toBeTruthy();
  });
});
