import { ExamDurationPipe } from './exam-duration.pipe';

describe('ExamDurationPipe', () => {
  it('create an instance', () => {
    const pipe = new ExamDurationPipe();
    expect(pipe).toBeTruthy();
  });
});
