import { InvalidOnTouchDirective } from './invalid-on-touch.directive';

describe('InvalidOnTouchDirective', () => {
  it('should create an instance', () => {
    const directive = new InvalidOnTouchDirective();
    expect(directive).toBeTruthy();
  });
});
